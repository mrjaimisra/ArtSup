<nav class="navbar navbar-default header-nav">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand nav-item-border navbar-logo" href="#">ArtSup</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href=<%= profile_path(current_user) %> class="nav-item-border">My Story</a></li>
        <li><a href=<%= users_collections_path(current_user) %> class="nav-item-border">My Collections</a></li>
        <li><a href=<%= users_path %> class="nav-item-border">Artists</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" id="avatar-holder">
            <% if current_user.image_url == "" %>
              <%= image_tag current_user.image_url, class: "navbar-avatar ima-rounded" %>
            <% else %>
              <%= image_tag current_user.avatar.url(:thumb), class: "navbar-avatar" %>
            <% end %>
            <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href=<%= profile_path %>>Profile</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="#"><%= current_user.name %></a href="#"></li>
            <li><a href="#"><%= current_user.email %></a></li>
            <li><a href=<%= logout_path %>>Logout</a></li>
          </ul>
        </li>
        <li><a href=<%= logout_path %> class="nav-item-border">Logout</a></li>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>